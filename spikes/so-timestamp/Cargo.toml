[package]
name = "spike-so-timestamp"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "驗證 macOS 上 SO_TIMESTAMP_MONOTONIC 的核心接收時間戳是否與 mach_absolute_time 同基準"

[[bin]]
name = "so-timestamp"
path = "src/main.rs"

[dependencies]
libc.workspace = true
mach2.workspace = true
